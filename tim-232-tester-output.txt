Here's what I found and did:

**Existing coverage (already present):**
- Happy path multi-setting save with all valid revisions — covered by "successfully applies multiple settings in one batch"
- Atomicity on conflict (stale revision in one setting rolls back others) — covered by "stale baseRevision in a batch rolls back other setting changes" 
- Single-setting path (`updateProjectSetting`) — covered by multiple tests

**Missing coverage (3 tests added):**
1. **Empty settings array** — was missing; `updateProjectSettings([])` should be a no-op, not an error. Now verified.
2. **Mixed set + delete both apply** — was missing; a batch combining a value-set and a delete (empty-string trigger) should apply both atomically. Now verified.
3. **Mixed set + delete rolls back when delete has stale revision** — was missing; if the delete in a mixed batch conflicts, neither the set nor the delete should apply. Now verified.

**Final counts:**
- `project_settings.remote.test.ts`: 27 → **30 tests**, all passing
- `review_issue_actions.remote.test.ts` + `review_thread_actions.remote.test.ts`: 46 tests, all passing (no regressions)
- `bun run check`: clean